name: excel_facility
description: A flutter and dart library for reading, creating, editing and
  updating excel sheets with compatible both on client and server side.
version: 1.0.0
homepage: https://github.com/vitoramaral10/excel

environment:
  sdk: '>=2.17.5 <3.0.0'

dependencies:
  archive: ^3.3.2
  equatable: ^2.0.5
  xml: ^6.1.0

dev_dependencies:
  test: ^1.21.6
